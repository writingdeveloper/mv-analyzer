import { fireEvent, render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { DiscoverPage } from './DiscoverPage'
import type { CorrelationsData, FeatureMeta, VideoRow } from '../data/schema'
const videos=[{video_id:'a',title:'Alpha MV',channel:'Chan A',domain:'vocaloid',group:'top',view_count:100,subscriber_count:10,view_per_sub:10,upload_date:'20260101',features:{scene_cuts_per_minute:20,audio_bpm:130}},{video_id:'b',title:'Beta MV',channel:'Chan B',domain:'vocaloid',group:'bottom',view_count:50,subscriber_count:10,view_per_sub:5,upload_date:'20260102',features:{scene_cuts_per_minute:5,audio_bpm:90}}] as VideoRow[]
const features=[{id:'scene_cuts_per_minute',label:'분당 컷 수',kind:'numeric',status:'formal'},{id:'audio_bpm',label:'BPM',kind:'numeric',status:'formal'}] as FeatureMeta[]
const correlations={method:'spearman',domains:{vocaloid:{features:['scene_cuts_per_minute','audio_bpm'],matrix:[[1,.5],[.5,1]]}}} as CorrelationsData
test('filters, changes feature and persists selected video details',()=>{render(<MemoryRouter><DiscoverPage videos={videos} features={features} correlations={correlations}/></MemoryRouter>);expect(screen.getByText('Alpha MV')).toBeInTheDocument();expect(screen.getAllByText(/z-score/i).length).toBeGreaterThan(0);expect(screen.getAllByText(/전체 100편 기준/).length).toBeGreaterThan(0);fireEvent.change(screen.getByLabelText('Y feature'),{target:{value:'audio_bpm'}});expect(screen.getAllByText('BPM').length).toBeGreaterThan(0);fireEvent.click(screen.getAllByRole('button',{name:/Alpha MV 선택/})[0]);expect(screen.getByRole('heading',{name:'Alpha MV'})).toBeInTheDocument();fireEvent.change(screen.getByLabelText('영상 검색'),{target:{value:'beta'}});expect(screen.getByText('1편 표시')).toBeInTheDocument()})
