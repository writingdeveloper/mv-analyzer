[pytest]
markers =
    slow: expensive regression tests excluded from the default fast QA suite
    media: tests that process real media fixtures
    gpu: tests that invoke CUDA, VLM, ASR, or other GPU workloads
    network: tests that require external network access
addopts = -m "not slow and not gpu and not network"
